<UserControl x:Class="CONEX_APP.Presentation.Views.Users.UserListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,10">
            <Button Content="Atrás" Command="{Binding GoBackCommand}" Width="80" Padding="5" Margin="0,0,20,0"/>
            <TextBlock Text="Tabla de Usuarios" FontSize="24" FontWeight="Bold" VerticalAlignment="Center"/>
        </StackPanel>
        
        <Button Grid.Row="1" Content="Añadir Nuevo Usuario" Command="{Binding OpenAddUserWindowCommand}" 
                HorizontalAlignment="Left" Padding="10,5" Margin="0,0,0,10" 
                Background="#007ACC" Foreground="White" FontWeight="Bold"/>

        <DataGrid Grid.Row="2" ItemsSource="{Binding Users}" AutoGenerateColumns="False" IsReadOnly="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="50"/>
                <DataGridTextColumn Header="Nombre" Binding="{Binding FullName}" Width="*"/>
                <DataGridTextColumn Header="Correo Electrónico" Binding="{Binding Email}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
