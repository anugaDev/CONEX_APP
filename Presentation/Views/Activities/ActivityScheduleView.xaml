<UserControl x:Class="CONEX_APP.Presentation.Views.Activities.ActivityScheduleView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,10">
            <Button Content="Atrás" Command="{Binding GoBackCommand}" Width="80" Padding="5" Margin="0,0,20,0"/>
            <TextBlock Text="Tabla de Clases y Actividades" FontSize="24" FontWeight="Bold" VerticalAlignment="Center"/>
        </StackPanel>
        
        <Button Grid.Row="1" Content="Añadir Nueva Actividad" Command="{Binding OpenAddUserWindowCommand}" 
                HorizontalAlignment="Left" Padding="10,5" Margin="0,0,0,10" 
                Background="#007ACC" Foreground="White" FontWeight="Bold"/>

        <DataGrid Grid.Row="2" ItemsSource="{Binding ActivitySchedule}" AutoGenerateColumns="False" IsReadOnly="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="40"/>
                <DataGridTextColumn Header="Nombre de Actividad" Binding="{Binding Name}" Width="*"/>
                <DataGridTextColumn Header="Tutor" Binding="{Binding Tutor}" Width="*"/>
                <DataGridTextColumn Header="Aula" Binding="{Binding Classroom}" Width="100"/>
                <DataGridTextColumn Header="Día y Hora" Binding="{Binding Date, StringFormat={}{0:dddd HH:mm}}" Width="150"/>
                <DataGridTextColumn Header="Apuntados / Máx" Binding="{Binding Occupancy}" Width="130"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
