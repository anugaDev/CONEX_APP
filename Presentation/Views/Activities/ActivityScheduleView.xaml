<UserControl x:Class="CONEX_APP.Presentation.Views.Activities.ActivityScheduleView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Cabecera -->
        <Grid Grid.Row="0" Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <StackPanel Grid.Column="0" Orientation="Horizontal">
                <Button Content="Atrás" Command="{Binding GoBackCommand}" Width="80" Padding="5" Margin="0,0,20,0"/>
                <TextBlock Text="Tabla de Clases y Actividades" FontSize="24" FontWeight="Bold" VerticalAlignment="Center"/>
            </StackPanel>
            <Button Grid.Column="1" Content="Ir a Usuarios ➔" Command="{Binding GoToUsersCommand}" 
                    HorizontalAlignment="Right" Padding="15,5" 
                    Background="#28A745" Foreground="White" FontWeight="Bold"/>
        </Grid>
        
        <!-- Barra de acciones -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,10">
            <Button Content="Añadir Nueva Actividad" Command="{Binding OpenAddUserWindowCommand}" 
                    Padding="10,5" Margin="0,0,10,0" 
                    Background="#007ACC" Foreground="White" FontWeight="Bold"/>
                    
            <Button Content="Eliminar Clase" Command="{Binding DeleteActivityCommand}" 
                    Padding="10,5" Margin="0,0,10,0"
                    Background="#CC0000" Foreground="White" FontWeight="Bold"/>
                    
            <Button Content="🖨️ Imprimir Hoja Asistencia" Command="{Binding PrintAttendanceCommand}" 
                    Padding="10,5" 
                    Background="#6C757D" Foreground="White" FontWeight="Bold"/>
        </StackPanel>

        <!-- Barra de búsqueda -->
        <Grid Grid.Row="2" Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBox Grid.Column="0"
                     Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                     Padding="8,5" FontSize="14"
                     ToolTip="Buscar por nombre de actividad, tutor o aula..."/>
            <Button Grid.Column="1" Content="✕" Width="32"
                    Command="{Binding ClearSearchCommand}"
                    Padding="5" Margin="4,0,0,0"
                    ToolTip="Limpiar búsqueda"/>
        </Grid>

        <!-- Tabla de actividades -->
        <DataGrid Grid.Row="3"
                  ItemsSource="{Binding ActivityScheduleView}"
                  SelectedItem="{Binding SelectedActivity, Mode=TwoWay}"
                  AutoGenerateColumns="False" IsReadOnly="True" SelectionMode="Single">
            <DataGrid.InputBindings>
                <MouseBinding MouseAction="LeftDoubleClick" Command="{Binding EditActivityCommand}" />
            </DataGrid.InputBindings>
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="40"/>
                <DataGridTextColumn Header="Nombre de Actividad" Binding="{Binding Name}" Width="*"/>
                <DataGridTextColumn Header="Tutor" Binding="{Binding Tutor}" Width="*"/>
                <DataGridTextColumn Header="Aula" Binding="{Binding Classroom}" Width="100"/>
                <DataGridTextColumn Header="Día y Hora" Binding="{Binding Date, StringFormat={}{0:dddd HH:mm}}" Width="150"/>
                <DataGridTextColumn Header="Apuntados / Máx" Binding="{Binding Occupancy}" Width="130"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
